trigger: none
pr: none

pool:
  vmImage: 'ubuntu-latest'

variables:
  NODE_VERSION: '22'
  PNPM_VERSION: '9.15.0'
  APP_SERVICE_NAME: 'app-payload'
  RESOURCE_GROUP: 'rg_nimbus_customerportal'
  AZURE_SUBSCRIPTION: 'new azure deploy for nimbus'  # Set this to your Azure subscription ID
  BUILD_OUTPUT_PATH: '$(System.DefaultWorkingDirectory)/apps/cms/.next'

stages:
  - stage: Build
    displayName: 'Build CMS App'
    jobs:
      - job: BuildJob
        displayName: 'Build and Test'
        steps:
          # Install Node.js
          - task: UseNode@1
            inputs:
              version: $(NODE_VERSION)
            displayName: 'Install Node.js'

          # Install pnpm
          - script: npm install -g pnpm@$(PNPM_VERSION)
            displayName: 'Install pnpm'

          # Install dependencies
          - script: pnpm install --frozen-lockfile
            displayName: 'Install dependencies'

          # Lint CMS app
          - script: pnpm --filter '@dtc/cms' lint
            displayName: 'Lint CMS app'
            continueOnError: true

          # Build CMS app
          - script: pnpm --filter '@dtc/cms' build
            displayName: 'Build CMS app'

          # Test CMS app (if tests exist)
          - script: pnpm --filter '@dtc/cms' test
            displayName: 'Run CMS tests'
            continueOnError: true

          # Package app + node_modules installed on the build agent
          - script: |
              DEST=$(Build.ArtifactStagingDirectory)/cms
              mkdir -p "$DEST"
              cp apps/cms/package.json "$DEST/package.json"
              cp apps/cms/next.config.mjs "$DEST/next.config.mjs"
              cp -r apps/cms/.next "$DEST/.next"
              cp -r apps/cms/public "$DEST/public" 2>/dev/null || true
              cd "$DEST"
              npm install --omit=dev --ignore-scripts --no-package-lock
            displayName: 'Prepare deployment artifacts'

          # Publish artifact
          - task: PublishBuildArtifacts@1
            inputs:
              pathToPublish: '$(Build.ArtifactStagingDirectory)/cms'
              artifactName: 'cms-app'
              publishLocation: 'Container'
            displayName: 'Publish artifacts'

  - stage: Deploy
    displayName: 'Deploy to Azure App Service'
    dependsOn: Build
    condition: succeeded()
    jobs:
      - job: DeployToAppService
        displayName: 'Deploy CMS to App Service'
        steps:
          # Download artifact
          - task: DownloadBuildArtifacts@0
            inputs:
              buildType: 'current'
              downloadType: 'single'
              artifactName: 'cms-app'
              downloadPath: '$(Pipeline.Workspace)'
            displayName: 'Download artifacts'

          # Deploy using Azure App Service Deploy task
          - task: AzureWebApp@1
            inputs:
              azureSubscription: $(AZURE_SUBSCRIPTION)
              appType: 'webAppLinux'
              appName: $(APP_SERVICE_NAME)
              runtimeStack: 'NODE|22-lts'
              startupCommand: 'node /home/site/wwwroot/node_modules/next/dist/bin/next start'
              package: '$(Pipeline.Workspace)/cms-app'
            displayName: 'Deploy to App Service'

          # Post-deployment note
          - script: echo "✓ Deployment complete. Payload migrations run automatically on startup."
            displayName: 'Deployment Summary'

          # Warm-up: hit the site to ensure the container handles its first request
          - script: |
              APP_URL="https://app-payload-ekhaedeqa9gugmfw.westeurope-01.azurewebsites.net/admin"
              echo "Warming up $APP_URL ..."
              for i in 1 2; do
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 "$APP_URL/admin")
                echo "Attempt $i: HTTP $STATUS"
                if [ "$STATUS" = "200" ] || [ "$STATUS" = "302" ] || [ "$STATUS" = "301" ]; then
                  echo "✓ Site is up."
                  exit 0
                fi
                echo "Not ready yet, waiting 60s..."
                sleep 60
              done
              echo "##[warning]Site did not respond with 2xx/3xx after 2 attempts — check App Service logs."
              exit 0
            displayName: 'Warm-up ping'

# Note: 
# 1. Environment variables (PAYLOAD_SECRET, DATABASE_URI, AZURE_STORAGE_* etc.) 
#    should be configured in Azure App Service → Configuration → Application settings
#    Never commit these to source control.
#
# 2. Payload CMS 3.x automatically runs migrations on startup.
#    node_modules are bundled from the build agent — no runtime install required.
#
# 3. After first deployment, create admin user via App Service Console:
#    cd /home/site/wwwroot/apps/cms
#    pnpm exec payload createUser --collection users --email admin@example.com --password YourSecurePassword
#
# 4. Access the admin dashboard at: https://<app-service-url>/admin
