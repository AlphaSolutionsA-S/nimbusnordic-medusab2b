# Merge Business Central Sales Orders and Sales Invoices in Order History

- **Date:** 2026-09-11
- **Type:** Story
- **Tracker:** JIRA — [NIMBUS-170](https://alphasolutionsdk.atlassian.net/browse/NIMBUS-170) (child of Epic NIMBUS-125)
- **Priority:** Medium
- **Project Folder:** issues/NIMBUS-170/
- **Updated by:** implementation-planner agent
- **Outcome:** Implementation plan is ready; implementation is the next stage. Wrote `manifest.md` and four task files (`01-bc-types-implementation.md`, `02-list-orders-merge-implementation.md`, `03-get-order-merge-implementation.md`, `04-storefront-updates-implementation.md`) plus `PLAN.md`. Test infrastructure already exists for both apps (backend `jest.config.js`, storefront `jest.config.ts`) — no Step 2c gate was triggered.
- **Handover to:** implementor agent
- **Handover prompt:** "Implement NIMBUS-170 using the plan in issues/NIMBUS-170/. Read manifest.md for the task order and dependencies, then implement each of the four task files in order (01 → 02 → 03 → 04): 01 updates apps/backend/src/modules/business-central/types.ts, 02 and 03 rewrite listOrders and getOrder in apps/backend/src/modules/business-central/service.ts and its test file, 04 updates two storefront files (apps/storefront/src/types/bc-order.ts and the bc-order-card component) plus its test. Each task file contains the exact code to write, the exact test cases to add/replace, and the build/lint/test commands to run before moving to the next task. Do not deviate from the join-key (order number, not id), pagination, or field-mapping decisions documented in each task and in PLAN.md — they were made deliberately to resolve open questions from SCOPE.md and are not to be re-derived."

---

- **Date:** 2026-09-11
- **Updated by:** implementor agent
- **Outcome:** All four tasks (01–04) implemented and validated; NIMBUS-170's merge of Business Central `salesOrders`/`salesInvoices` into `listOrders` and `getOrder`, plus decision D14 (`BCOrderDetail.invoices`), is complete on both backend and storefront. `manifest.md` and all four task files updated to `DONE`.
  - **Task 01:** Rewrote `apps/backend/src/modules/business-central/types.ts` per spec (widened `status` to `string`, added `BCOrderInvoiceStatus`/`invoiceStatus`, added `BCOrderInvoiceSummary`/`BCOrderDetail.invoices`, renamed `BCGetOrderParams.orderId` → `orderNumber`). Confirmed no remaining `BCOrderStatus` references.
  - **Task 02:** Rewrote `listOrders` in `apps/backend/src/modules/business-central/service.ts` (small-table-first pagination merging `salesOrders` with `salesInvoices`, dedup by order number, guardrail constants). Replaced the `listOrders` describe block in `apps/backend/src/modules/business-central/__tests__/service.spec.ts`.
  - **Task 03:** Rewrote `getOrder` in the same `service.ts` (number-based, parallel lookup across both entity sets, line merge, D14 `invoices` field). Updated `apps/backend/src/api/store/bc-orders/[id]/route.ts` (`orderId` → `orderNumber`). Replaced the `getOrder` describe block in the same test file.
  - **Task 04:** Updated `apps/storefront/src/types/bc-order.ts` (widened `status`, added `invoiceStatus`/`invoices`) and `apps/storefront/src/modules/account/components/bc-order-card/index.tsx` (details link now routes by `encodeURIComponent(order.number)`). Replaced `apps/storefront/src/__tests__/modules/account/components/bc-order-card/index.test.tsx`.
- **Deviations from the literal task files (smallest compatible corrections, reported per the implementor's mandate):**
  1. Task 02's TC-1 test fixture was internally inconsistent — with `limit: 20` and only 1 order returned, `remainder = 19 > 0` would trigger the invoice-fill path, but the fixture only mocked 4 fetch calls and asserted exactly 4 (matching its own "no invoice fill needed" comment). Changed the fixture's `limit` from `20` to `1` (and the expected result's `limit` to match) so the page is genuinely fully satisfied — production merge logic was left exactly as specified.
  2. Task 03's file-impact analysis missed a real caller: `apps/backend/src/workflows/business-central-return/steps/prepare-bc-return.ts` calls `bcService.getOrder({ orderId: input.sourceSalesOrderId, ... })`, where `sourceSalesOrderId` is a genuine salesOrder GUID (per D6, submitted by the storefront's out-of-scope return flow via `order.id`, not a URL param). Renaming this call's field to `orderNumber` would have compiled but silently broken returns (a GUID would never match `number eq`). Added a small additive method, `getOrderBySalesOrderId(params: BCGetOrderBySalesOrderIdParams)`, to `IBusinessCentralModuleService`/`BusinessCentralModuleService` that preserves the exact pre-NIMBUS-170 id-based, `salesOrders`-only lookup (no invoice merge, `invoices: []`), and pointed `prepare-bc-return.ts` at it instead of `getOrder`. `getOrder`'s new number-based contract (Tasks 01/03) is otherwise unchanged. No existing test covered this step; none needed updating.
- **Validation performed:**
  - `cd apps/backend && pnpm test:integration:modules --testPathPattern=business-central` — 38/38 passed (includes `listOrders`, `getOrder`, `getCustomer`, return-stub suites).
  - `cd apps/backend && pnpm build` — backend and frontend compile successfully, no errors (12 pre-existing, unrelated lint warnings only).
  - `cd apps/storefront && pnpm test` — 247/248 passed; the 1 failure (`src/__tests__/app/main-layout.test.tsx`, promo-banner copy) is pre-existing and unrelated — confirmed via `git status` that no file in that area was touched.
  - `cd apps/storefront && pnpm test -- --testPathPattern="bc-order"` — 9/9 passed (bc-order-card, -overview, -filters, -return, detail-template).
  - `pnpm lint` (repo root, all 3 packages) — 0 errors; only pre-existing, unrelated warnings.
  - `cd apps/storefront && npx tsc --noEmit -p tsconfig.json` — pre-existing type errors only in unrelated files (`account-nav`, `company-card-bc-readonly`, `profile-card`, `cart-drawer`); none in `bc-order.ts`, `bc-order-card`, or any other file this story touched. Run in place of `next build`'s type-check because `next.config.js` sets `typescript.ignoreBuildErrors: true` (pre-existing), and `pnpm build` for the storefront separately fails during static-generation page-data collection for `/[countryCode]/categories/[...category]` with `fetch failed` — an environment/connectivity issue (no reachable backend during this build), unrelated to this story's changes.
- **Jira:** NIMBUS-170 was at **Estimation** at the start of this session (no direct transition to In Progress existed). Transitioned Estimation → To Do ("Estimate approved", id 531) → In Progress ("Start work", id 111) before starting edits. After all four tasks validated, transitioned To Do... → **Internal Review** (see transition applied after this entry).
- **Next owner:** none — all four tasks in `manifest.md` are DONE and the verification checklist in `PLAN.md` is satisfied (modulo the two pre-existing, unrelated failures noted above). Ready for human/code review. If a reviewer wants the storefront's full `next build` (including static generation) verified, it needs to run against a reachable backend instance.
- **Handover prompt:** "NIMBUS-170 implementation is complete (Tasks 01–04, all validated) and the Jira issue is in Internal Review. Review the diff for the four task areas (business-central types/service, the bc-orders API route, the return-flow compatibility fix in prepare-bc-return.ts, and the storefront bc-order-card/types), then close out via the normal review/merge process. No further implementor action is expected unless review turns up a defect."
